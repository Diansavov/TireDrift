@model TireDrift.Models.ViewModels.SupplierViewModel

@{
    ViewData["Title"] = "Tires";
}

<form asp-action="AddSupplier" enctype="multipart/form-data">
    <h2 class="form-title">Add Supplier</h2>
    <div class="inputs-container">
        <div class="input-field">
            <input type="text" placeholder="Име" asp-for="Name">
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>
        <div class="input-field">
            <input type="text" placeholder="Цена" asp-for="PhoneNumber">
            <span asp-validation-for="PhoneNumber" class="text-danger"></span>
        </div>
        <div class="submit-button">
            <button class="btn btn-primary" type="submit">Добавяне на доставчик</button>
        </div>
</form>